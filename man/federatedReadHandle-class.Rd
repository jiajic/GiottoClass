% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-federatedRead.R
\docType{class}
\name{federatedReadHandle-class}
\alias{federatedReadHandle-class}
\title{S4 federatedReadHandle}
\value{
a \code{federatedReadHandle}
}
\description{
Lazy wrapper around per-sample data fragments. Holds the list of
fragments plus a \code{combine} function that knows how to fold them into
a single output object of the requested type.
}
\section{Slots}{

\describe{
\item{\code{substores}}{\code{list} — per-sample fragments (one entry per sample).
Fragments can be any class the \verb{@combine} function knows how to
consume: matrices, data.tables, exprObjs, arrow queries, parquet
substores, etc.}

\item{\code{keys}}{\code{character} — sample names. Same length as \verb{@substores};
names of \verb{@substores} may be NULL for unnamed lists, in which case
\verb{@keys} is the authoritative ordering.}

\item{\code{output_class}}{\code{character(1)} — hint for the default
materialization shape (\code{"matrix"}, \code{"data.table"}, \code{"exprObj"},
\code{"cellMetaObj"}, ...). The consumer may override via the
\code{materialize()} \verb{as = } argument.}

\item{\code{combine}}{\code{function} — folds \verb{@substores} into a single object.
Signature: \verb{function(substores, keys, ...)}. Receives the fragment
list and the key vector; returns the merged object.}

\item{\code{meta}}{\code{list} — opaque metadata for the combine function (e.g.
feature intersection mode for expression matrices, column overlap
rules for cmeta data.tables). Combine functions consume this via
\code{attr(combine_result, "meta")} or by inspecting the slot directly
when called through \code{materialize()}.}
}}

