% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-federatedRead.R
\name{federatedReadHandle}
\alias{federatedReadHandle}
\title{Construct a federatedReadHandle}
\usage{
federatedReadHandle(
  substores,
  keys = names(substores),
  output_class = NA_character_,
  combine,
  meta = list()
)
}
\arguments{
\item{substores}{per-sample list of data fragments}

\item{keys}{character vector of sample names (defaults to
\code{names(substores)})}

\item{output_class}{default output class hint (one of \code{"matrix"},
\code{"data.table"}, \code{"exprObj"}, \code{"cellMetaObj"}, ...). \code{NA} means the
consumer must request via \code{materialize(handle, as = ...)}.}

\item{combine}{\verb{function(substores, keys, ...)} that folds
\verb{@substores} into one object}

\item{meta}{optional \code{list} of metadata for the combine function}
}
\value{
a \code{federatedReadHandle}
}
\description{
Wrap a per-sample list of fragments + a folder function
into a federatedReadHandle. The handle is cheap to hold; consumers
call \code{\link[=materialize]{materialize()}} (or \code{[}/\code{[[}-extraction) when they need a
concrete object.
}
