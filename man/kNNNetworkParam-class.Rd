% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NN_network.R
\docType{class}
\name{kNNNetworkParam-class}
\alias{kNNNetworkParam-class}
\alias{kNNNetworkParam}
\title{kNNNetworkParam — k-Nearest-Neighbour Network Param}
\usage{
kNNNetworkParam(
  k = 30L,
  filter = FALSE,
  maximum_distance = NULL,
  minimum_k = 0L,
  weight_fun = function(d) 1/(1 + d),
  include_weight = TRUE,
  include_distance = TRUE,
  output = c("auto", "data.table", "igraph", "parquet"),
  engine = c("auto", "dbscan", "hnsw")
)
}
\arguments{
\item{k}{number of neighbours}

\item{filter}{apply \code{minimum_k}/\code{maximum_distance} post-filter}

\item{maximum_distance}{maximum edge length}

\item{minimum_k}{minimum neighbours per node when filtering}

\item{weight_fun}{function mapping distance to weight}

\item{include_weight, include_distance}{include columns in output}

\item{output}{one of \code{"auto"}, \code{"data.table"}, \code{"igraph"}, \code{"parquet"}}

\item{engine}{character. kNN search backend, one of \code{"auto"} (default),
\code{"dbscan"} or \code{"hnsw"}. \code{"dbscan"} is an exact kd-tree search -- optimal
on the 2-3 dimensions of a spatial network, and degrading toward brute
force as dimensionality rises. \code{"hnsw"} is an approximate HNSW index
(hnswlib, via \pkg{RcppHNSW}) whose cost is insensitive to dimensionality,
making it much
faster in the 10-1000 dimensions of an expression-space network. \code{"auto"}
leaves the choice to the caller that knows which space the network is
built in; see \code{\link[=createNetwork]{createNetwork()}}.}
}
\description{
Constructor and class for k-Nearest-Neighbour network parameters. Pass
to \code{\link[=createNetwork]{createNetwork()}} to build a kNN graph. kNN edges are inherently
asymmetric (\code{a}'s k-nearest neighbours are not necessarily those for
which \code{a} is among the k-nearest), so the resulting graph is
\strong{directed} when promoted to igraph.
}
\section{Slots}{

\describe{
\item{\code{k}}{integer. number of nearest neighbours per node.}

\item{\code{filter}}{logical. apply \code{minimum_k}/\code{maximum_distance} post-filter.}

\item{\code{maximum_distance}}{numeric or NULL. drop edges longer than this.}

\item{\code{minimum_k}}{integer. minimum neighbours per node when filtering.}

\item{\code{weight_fun}}{function. weight = \code{weight_fun(distance)}.}

\item{\code{include_weight,include_distance}}{logical. include columns.}

\item{\code{output}}{character. one of \code{"auto"}, \code{"data.table"}, \code{"igraph"},
\code{"parquet"}. See \code{\link[=createNetwork]{createNetwork()}}.}
}}

